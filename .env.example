# ─────────────────────────────────────────────
# Pars Studio — environment placeholders
# Part 1 (landing page) needs NONE of these.
# These are reserved for Part 2 — the booking system.
# Copy this file to `.env.local` when Part 2 is added.
# ─────────────────────────────────────────────

# Supabase (added in part 2 — bookings table + admin auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Resend (added in part 2 — booking confirmation + studio notification emails)
RESEND_API_KEY=
STUDIO_NOTIFICATION_EMAIL=
